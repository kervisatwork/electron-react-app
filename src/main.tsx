import React from "react";
import ReactDOM from "react-dom/client";
import AppReact from "./components/App/App";

ReactDOM.createRoot(
  document.getElementById("root")!
).render(
  <React.StrictMode>
    <AppReact />
  </React.StrictMode>
);